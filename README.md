# certs
ISKCON Certificates

## Installation
1. install [jwilder/nginx-proxy](https://hub.docker.com/r/jwilder/nginx-proxy) sharing **proxy** network
2. git checkout
3. cp initial_templates templates
4. cp .env.example .env (set your host)
5. docker compose up -d
